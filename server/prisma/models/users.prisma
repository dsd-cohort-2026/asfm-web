model User {
  id          String       @id @default(uuid())
  first_name   String
  last_name   String
  email       String    @unique
  phone       String
  address     String
  role        Role      @default(USER)
  created_at  DateTime  @default(now())
}