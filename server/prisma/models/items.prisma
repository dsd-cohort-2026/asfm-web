model Items {
  id          String        @id @default(uuid())
  category    Item_Category
  name        String
  brand       String
  description String
  species     Species
  unit        String
  is_active   Boolean
  created_at  DateTime      @default(now())
}
